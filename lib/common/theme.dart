import 'package:flutter/material.dart';

final appTheme = ThemeData(
  primarySwatch: Colors.red,
  secondaryHeaderColor: const Color(0xff0080FF),
);

class AppColors {
  static const Color primaryColor = Color(0xffFF3A44);
}
